apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubenest-workload.fullname" . }}-test-connection"
  labels:
    {{- include "kubenest-workload.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubenest-workload.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
